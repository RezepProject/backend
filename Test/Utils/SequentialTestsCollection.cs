/*using Test;
using Xunit;

[CollectionDefinition("Sequential Tests", DisableParallelization = true)]
public class SequentialTestsCollection : ICollectionFixture<TestSetup>
{
    // Same as before: this class is never instantiated,
    // it simply provides the collection definition.
} */